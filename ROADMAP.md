# ROADMAP · RUSTIDE ARTFORGE

当前开放：**R1 角色立绘（赛璐璐平涂人设）**。
以下路由已在骨架里预留位置，共用同一份世界观、调色板与角色档案。

---

## 设计原则：档案是唯一真源

```
        照片
         ↓
   [R1 角色立绘]
         ↓
  角色档案 + payload.json   ← 唯一真源
         ↓
  ┌──────┼──────┬──────┐
 R2      R3     R4    (更多)
像素小人  海报   场景道具
```

任何下游路由**不重新设计角色**，只读 `payload.json` 换渲染规格。
这样同一个角色的立绘、像素小人、海报里的样子必然一致。

---

## R2 · 像素小人（sprite）🟡 进行中

待补内容：
- sprite 尺寸档（风来之国主角约 24×40 px；本作待定）
- 从 `payload.palette` 降采样到像素调色板的规则（每个 HEX → 2–3 阶）
- 像素层的描边规则（是否保留暖褐边、是否用选择性描边）
- 朝向数（4 向 / 8 向）、行走帧数、待机帧数
- 立绘 → 像素的**特征保留优先级**（剪影歪点 > 主强调色 > 遮挡物 > 服装层次）
- 出图提示词模板 + 负面清单（像素专用：禁止抗锯齿、禁止渐变、禁止半透明）

## R3 · 海报 / 主视觉 🔒

待补内容：
- 风来之国有**两套**海报画风，需先选定：厚涂手绘系 vs 平涂丝网/漫画系
- 构图模板（单人主视觉 / 群像 / 场景叙事）
- 排版与字体规则（旧世字样、汛期公告体）
- 与 R1 赛璐璐画风的关系：**海报可以另一套渲染，但角色形体与配色必须锁死**

## R4 · 场景 / 道具 🔒

待补内容：
- 四个地点的建筑语言（桥城 / 釜谷 / 岸线 / 上环）
- 蒸汽基础设施的视觉词汇（管道、气笛、压力表）
- 旧世遗物的设计规则（什么该保留、什么该锈掉）
- 道具的"来历可追溯"规则

---

## 世界观扩展位

`references/world-rustide.md` 目前覆盖：时间轴、灾变、技术水位、地理、社会、基调。
后续可补：语言与文字、货币与交易、食物、音乐与广播、节庆（收日 / 开风）、旧世传说、具体历史事件。
